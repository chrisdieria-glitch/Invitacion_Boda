# Variables de entorno para `npm run build` (modo production).
#
# Vite carga este archivo (y tiene más prioridad que .env) cuando se ejecuta
# `npm run build`, de modo que la build que se sube a GitHub Pages siempre
# apunta al backend de Render y nunca a localhost.
VITE_API_URL=https://invitacion-boda-1-wt5o.onrender.com
